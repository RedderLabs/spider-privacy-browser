<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 11+ package visibility: allow querying/launching Orbot (Tor).
         Without this, getPackageInfo/getLaunchIntentForPackage silently fail. -->
    <queries>
        <package android:name="org.torproject.android" />
    </queries>

    <!-- Required to run our own VpnService for in-app DoH, kept alive as a
         foreground service (special-use: an encrypted-DNS tunnel). POST_NOTIFICATIONS
         lets its ongoing notification show on Android 13+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Register as a browser so the app can be set as the default handler
             for web links and catch http/https URLs tapped in other apps.
             No host + BROWSABLE + http & https = Android lists it as a "Browser app". -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
            <data android:scheme="https" />
        </intent-filter>
      </activity>

      <!-- In-app DoH tunnel. BIND_VPN_SERVICE + the VpnService intent-filter
           are mandatory for a VpnService. foregroundServiceType=specialUse (with
           the declared subtype) is required on Android 14+ to call startForeground. -->
      <service
        android:name=".dns.DnsVpnService"
        android:permission="android.permission.BIND_VPN_SERVICE"
        android:foregroundServiceType="specialUse"
        android:exported="false">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="encrypted_dns_doh_tunnel" />
        <intent-filter>
          <action android:name="android.net.VpnService" />
        </intent-filter>
      </service>

      <!-- WireGuard tunnel (Phase 5b). The GoBackend VpnService is declared by
           the wireguard-android AAR; we merge in a special-use FGS type + subtype
           so it can call startForeground on Android 14+ (matching our DoH VpnService). -->
      <service
        android:name="com.wireguard.android.backend.GoBackend$VpnService"
        android:foregroundServiceType="specialUse"
        tools:replace="android:foregroundServiceType">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="wireguard_vpn_tunnel" />
      </service>
    </application>
</manifest>
