# Development overrides. Same keys as .env (see that file for docs).
# react-native-dotenv reads `.env` by default; point it here with
# DOTENV_CONFIG_PATH / an env-specific setup if you want these to apply.
PRIVACY_JS_ENABLED=true
CONTENT_BLOCKING_ENABLED=true
DEFAULT_DOH_PROVIDER=mullvad
